const reducer = (state = null, action) => {
  switch (action.type) {
    case "ADD_GUITAR":
      return {};
    case "EDIT_GUITAR":
      return {};
    default:
      return state;
  }
};

export default reducer;
